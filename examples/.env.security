# Security Configuration Example
# This file demonstrates available security options for the AutoRAG framework
# Copy and modify for your deployment needs

# ============================================================================
# FRAMEWORK DEFAULT MODE (Permissive - for evaluation/demo)
# ============================================================================
# Leave all security variables unset for permissive defaults
# This is suitable for:
# - Local development
# - Testing and evaluation
# - Demo deployments
# - Proof of concepts

# ============================================================================
# PRODUCTION MODE (Restrictive - for production deployments)
# ============================================================================

# CORS Configuration
# Restrict API access to specific origins (comma-separated)
# When unset: Allows all origins (*)
ALLOWED_ORIGINS=https://app.example.com,https://www.example.com

# Rate Limiting
# Prevent abuse by limiting requests per client
# When unset: No rate limiting
ENABLE_RATE_LIMITING=true
RATE_LIMIT_WINDOW_MS=60000    # 1 minute window
RATE_LIMIT_MAX_REQUESTS=60    # 60 requests per minute

# Content Security Policy
# Enable strict security headers for production
# When unset: Permissive CSP for easy embedding
ENABLE_STRICT_CSP=true

# Request Size Limit
# Maximum request body size in KB
# Always enabled for stability (default: 100KB)
MAX_REQUEST_SIZE_KB=50

# ============================================================================
# STAGING/DEMO MODE (Balanced - for public demos)
# ============================================================================
# Example configuration for public-facing demos:
#
# ENABLE_RATE_LIMITING=true
# RATE_LIMIT_MAX_REQUESTS=200   # More generous for demos
# # Leave ALLOWED_ORIGINS unset to allow embedding from anywhere
# # Leave ENABLE_STRICT_CSP unset for maximum compatibility

# ============================================================================
# INTERNAL TOOLS MODE (Moderate - for internal applications)
# ============================================================================
# Example configuration for internal company tools:
#
# ALLOWED_ORIGINS=https://internal.company.com,https://tools.company.com
# ENABLE_RATE_LIMITING=true
# RATE_LIMIT_MAX_REQUESTS=100
# # CSP can be relaxed for internal tools if needed

# ============================================================================
# NOTES
# ============================================================================
# 1. This is an open-source framework designed for flexibility
# 2. Default settings are permissive to facilitate evaluation
# 3. Always use HTTPS (enforced by Cloudflare Workers)
# 4. Review and adjust settings based on your security requirements
# 5. Consider implementing authentication for sensitive deployments
# 6. R2 browser endpoints include DELETE for testing - secure in production