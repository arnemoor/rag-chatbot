# AutoRAG Environment Configuration
# 
# QUICK START: Copy this file to .env and update with your values
#
# MORE EXAMPLES: See examples/ folder for:
#   - .env.basic     - Minimal setup (Workers AI only, no API keys needed)
#   - .env.production - Production setup with security settings
#   - .env.enterprise - Enterprise setup with GPT-5 and Claude 4.x
#   - .env.security  - Security-focused configuration
#
# ============================================
# REQUIRED CONFIGURATION
# ============================================

# Project Names (used for deployment)
WORKER_NAME=autorag-worker
PAGES_PROJECT_NAME=autorag-widget

# Cloudflare Account and Authentication
CLOUDFLARE_ACCOUNT_ID=your-account-id-here
CLOUDFLARE_API_TOKEN=your-api-token-here

# Cloudflare AutoRAG instance name
AUTORAG_INSTANCE_ID=your-autorag-instance

# AI Gateway name (will be created if doesn't exist)
AI_GATEWAY_NAME=autorag-gateway

# ============================================
# OPTIONAL CONFIGURATION
# ============================================

# R2 Bucket name for document storage
R2_BUCKET_NAME=library-docs-01

# Environment type (development, staging, production)
ENVIRONMENT=development

# Debug mode (set to false in production)
DEBUG_MODE=true

# ============================================
# EXTERNAL AI PROVIDERS (Optional)
# ============================================
# The deploy script will automatically set these as secrets

# OpenAI API Key - Enables GPT-5, GPT-4o models
# OPENAI_API_KEY=sk-...your-openai-key...

# Anthropic API Key - Enables Claude 4.x, Claude 3.x models  
# ANTHROPIC_API_KEY=sk-ant-...your-anthropic-key...

# ============================================
# SECURITY (Optional for Production)
# ============================================

# Allowed origins for CORS (comma-separated)
# Leave empty for wildcard (*) in development
# ALLOWED_ORIGINS=https://yourdomain.com,https://app.yourdomain.com